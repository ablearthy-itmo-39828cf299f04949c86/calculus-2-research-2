name: Build LaTeX

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ablearthy/docker-latex:1.0.0
      volumes:
        - .:/document

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: ls 
